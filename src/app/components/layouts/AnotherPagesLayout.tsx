'use client'

const AnotherPagesLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <main className="main">
      {children}
    </main>
  )
}

export default AnotherPagesLayout